# Example environment variables for docker-compose and backend
# Copy this file to .env and set secure values for production

# Backend (Spring Boot)
SPRING_PROFILES_ACTIVE=prod
SERVER_PORT=8089

# Database (used by docker-compose backend to connect to MySQL service)
DB_HOST=mysql
DB_PORT=3306
DB_NAME=db_improvement_solutions
DB_USER=admin
DB_PASSWORD=root

# JWT secret for token signing (CHANGE in production)
JWT_SECRET=change-me-please-very-long-random-string

# MySQL container initialization (optional overrides)
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=db_improvement_solutions
MYSQL_USER=admin
MYSQL_PASSWORD=root

# Frontend exposed ports (from docker-compose)
FRONTEND_HTTP_PORT=8001
FRONTEND_HTTPS_PORT=8443
